function f = LBA_wpdf(z, t, A, b, v, sv)
% Get PDF of activation values z of ith accumulator at time t in LBA model assuming no
% boundaries
% f = LBA_wpdf(z, t, A, b, v, sv)
%
%
% SF 2012

g = normcdf((z-A)./t, v, )
h = (b-t.*v)./(t.*sv);

f = (-v.*normcdf(g) + sv.*normpdf(g) + v.*normcdf(h) - sv.*normpdf(h))./A;